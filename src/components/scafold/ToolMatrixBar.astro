<header class="matrix-brand-bar" data-matrix-brand-bar>
  <div class="matrix-brand-bar__inner">
    <a class="matrix-brand-bar__brand" href="https://www.gotoolmatrix.com/" aria-label="Smart Tool Matrix home">
      <span class="matrix-brand-bar__mark">S</span>
      <strong>Smart Tool Matrix</strong>
    </a>
    <span class="matrix-brand-bar__section">Website Inspection</span>
    <a class="matrix-main-return" href="https://www.gotoolmatrix.com/">
      <span aria-hidden="true">←</span>
      <span class="matrix-main-return__en">Back to main site</span>
      <span class="matrix-main-return__zh">返回主站</span>
    </a>
    <a class="matrix-brand-bar__home" href="/seo/">All website checks</a>
  </div>
</header>

<style lang="scss">
  .matrix-brand-bar { position: sticky; top: 0; z-index: 50; height: 64px; border-bottom: 1px solid var(--border-color); background: color-mix(in srgb, var(--background) 86%, transparent); backdrop-filter: blur(18px); }
  .matrix-brand-bar__inner { width: min(100% - 2rem, 80rem); height: 100%; margin: 0 auto; display: flex; align-items: center; gap: .75rem; }
  .matrix-brand-bar__brand { display: inline-flex; align-items: center; gap: .65rem; color: var(--text-color); text-decoration: none; }
  .matrix-brand-bar__mark { display: grid; width: 34px; height: 34px; place-items: center; border-radius: 10px; color: #fff; font-weight: 800; background: linear-gradient(135deg, #2563eb, #0891b2); }
  .matrix-brand-bar__section { padding: .35rem .65rem; border: 1px solid var(--border-color); border-radius: 999px; color: var(--text-color-secondary); font-size: .75rem; font-weight: 700; }
  .matrix-brand-bar__home { margin-left: auto; color: var(--text-color-secondary); font-size: .875rem; font-weight: 650; text-decoration: none; }
  .matrix-brand-bar__home:hover { color: var(--primary); }
  .matrix-main-return { display: inline-flex; align-items: center; gap: .4rem; padding: .5rem .75rem; border: 1px solid color-mix(in srgb, var(--primary) 48%, var(--border-color)); border-radius: .75rem; color: var(--primary); background: color-mix(in srgb, var(--primary) 10%, transparent); font-size: .8rem; font-weight: 700; line-height: 1; text-decoration: none; }
  .matrix-main-return:hover { border-color: var(--primary); background: color-mix(in srgb, var(--primary) 16%, transparent); }
  .matrix-main-return__zh { display: none; }
  html[lang^='zh'] .matrix-main-return__en { display: none; }
  html[lang^='zh'] .matrix-main-return__zh { display: inline; }
  html[data-embedded='true'] .matrix-brand-bar { display: none; }
  @media (max-width: 520px) {
    .matrix-brand-bar__brand strong, .matrix-brand-bar__section { display: none; }
    .matrix-main-return__en { font-size: 0; }
    .matrix-main-return__en::after { content: 'Main'; font-size: .8rem; }
    html[lang^='zh'] .matrix-main-return__zh { font-size: 0; }
    html[lang^='zh'] .matrix-main-return__zh::after { content: '主站'; font-size: .8rem; }
  }
</style>
